#include<stdio.h>
int main()
{
	int l,w,area; 
	printf("enter the length of rectangle");
	scanf("%d",&l);
	printf("enter the width of rectangle");
	scanf("%d",&w);
	area=l*w;
	printf("here is the area of ur rectangle is %d",area);
	return 0;
}
